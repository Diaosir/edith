import File from 'edith-types/lib/file'

export default {
    namespace: 'jest',
    state: {
    },
    effects: {
        *onMessage({ payload }, { call, put }) {
          
        }
    },
    reducers: {
    }
}