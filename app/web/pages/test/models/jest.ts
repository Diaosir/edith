import File from '@/datahub/project/entities/file'

export default {
    namespace: 'jest',
    state: {
    },
    effects: {
        *onMessage({ payload }, { call, put }) {
          
        }
    },
    reducers: {
    }
}